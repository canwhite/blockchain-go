## Byzantine Fault Tolerance

拜占庭将军问题是分布式系统中达成共识的难题，将军们需统一行动，但可能有叛徒传递错误信息。工作量证明（PoW）通过计算难题解决此问题：一个营地解决 PoW 后，广播结果，其他营地接受并延续此消息链，丢弃竞争消息。若解决营地篡改数据，其他营地可快速识别错误哈希，拒绝其结果，转而接受下一个解决营地的消息。这激励诚实行为，因为篡改者浪费算力无回报，从而保证系统一致性和安全性

> It’s important to note here that Proof of Work does not care about the message itself, only that the nodes agreed to the final message.  
> PoW 的核心：达成共识

## Smart Contracts & Turing Completeness

- Smart Contracts  
  如果说比特币是跨国币的话，可以把 Ethereum 看作跨国合同
- Turing Completeness  
  是说 Solidity 是一门具有完全功能的编程语言，能在 Ethereum Blockchain 上编写合约

## Delegated Proof of Stake (DPOS)

持币者投票选出 21~101 个节点，按顺序轮流出块，3 秒确认，高效节能，支持高 TPS，适合商用链，兼顾去中心化与性能
DPoS 收益分配 = 节点奖励 + 投票者分红 + 生态基金，具体规则由链上治理决定。

## State Channels / Side Chains

- State Channels、
  状态通道同理，链下高频交互，最终结算回链，当然他人可以随时提出异议，避免主链拥堵与高昂 Gas。
  状态通道本身不直接拥有或托管链下资产。它的作用是让参与方在链下进行多次交互，只有最终结果才提交到主链结算。
- Side Chains
  侧链是主链的平行链，可扩展性高，但需要主链支持，如以太坊的 Plasma，侧链与主链交互时，需要主链验证，消耗 Gas，不适合高频交易。
  侧链和主链交互的核心是“资产”和“数据”的跨链转移。
  最常见的是资产跨链：用户将主链上的资产锁定在一个智能合约中，侧链收到锁定证明后，在侧链上生成等值资产（如映射币）；
  反之，用户在侧链销毁映射资产，主链合约解锁原生资产。
  数据交互则包括状态同步、事件通知等，通常通过中继、验证者或跨链桥实现。整个过程依赖于主链的安全性和验证机制，确保资产不会被双花或丢失。
- VS
  总结：状态通道是链下交互、链上结算，适合高频小额、参与方信任场景；侧链是独立链，适合功能扩展和大规模应用，但安全性需重点关注。

## Payment Channels

支付通道（Payment Channel）是一种链下扩容技术，允许两方在区块链外频繁交易，仅在通道开启和关闭时上链结算。这样大幅降低手续费和延迟，适合高频小额支付。典型应用如闪电网络，保证安全前提下提升区块链吞吐量。

## Sum Up

其实无论是状态通道（state channel）还是支付通道（payment channel），本质上都需要参与方之间有一定的信任基础，属于“部分中心化”——即便不依赖单一中心节点，但通道内的交互和结算规则需要双方或多方共同维护和约束。如果一方作恶，虽然可以通过链上仲裁保护权益，但过程依赖于链下协议的正确执行和参与方的配合。

相比之下，侧链（side chain）虽然也有一定的信任假设（如侧链自身的安全性、验证者的诚实性），但其运行机制更接近于一条独立的区块链，参与方更分散，安全性更多依赖于主链和侧链的共识机制。侧链的“中心化感”会相对弱一些，尤其是在采用去中心化验证者或跨链桥的设计时，用户对单一方的信任依赖更低。

总结：状态通道和支付通道适合高频、熟人间的小额交互，信任基础较强，部分中心化特征明显；侧链则更适合大规模、功能扩展场景，去中心化程度更高，但安全性和资产跨链风险需重点关注。
